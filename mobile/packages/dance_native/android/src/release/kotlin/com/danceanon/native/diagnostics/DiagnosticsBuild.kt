package com.danceanon.native.diagnostics

/** Release source set intentionally compiles diagnostics out of the product runtime. */
internal object DiagnosticsBuild {
    const val ENABLED: Boolean = false
}
